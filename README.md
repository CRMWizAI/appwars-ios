# AppWars iOS
Native SwiftUI app for AppWars tournament platform.

## Setup
1. Open `AppWars.xcodeproj` in Xcode
2. Wait for Swift packages to resolve
3. Enable "Sign in with Apple" capability in Signing & Capabilities
4. Build and run
